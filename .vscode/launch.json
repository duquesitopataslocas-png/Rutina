{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Expo Web (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:19006",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm: web",
      "postDebugTask": "terminate expo web"
    }
  ]
}
